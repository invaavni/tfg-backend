## How to install ##

For install all dependences execute:

`pip install -r requirements.txt`

Set environment variables:

`export FLASK_CONFIG=development` or `export FLASK_CONFIG=production`

`export FLASK_APP=run.py`

Run server:

`flask run`

Check lintern:

`pylint app/`